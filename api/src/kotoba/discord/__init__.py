"""Her Discord surface: a fourth way in, beside the web, the ElevenLabs tunnel and the terminal.

Nothing is imported here on purpose. `discord.py` is an optional extra and the tool modules must stay
importable without it, so every module that touches the library imports it inside a function.
"""
