"""Kotoba — an AI companion you can talk to, that can actually do things."""

__version__ = "0.10.0"

#: What `pip install` is given. The import, the command and her name are all `kotoba`; only the
#: distribution differs, because that one word is taken on the index by somebody else's library.
DIST_NAME = "kotoba-companion"
